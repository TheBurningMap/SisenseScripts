# hostname property
hostName=sand004
# Folder Name
gitfolder=Dashboards
# git repo to integrate dashboards
gitrepo="git@github.build.ge.com:DashboardsBackup/SAND004_DASHBOARDS_BACKUP.git"
# git branch name
gitbranch=develop
# dash list if specific list of dashboards to be synced (This field need to be configured only when running "pushSpecificDash.ps1")
dashList=
#foldeId reads the ID of a folder from the user input and preserve it to the folderId field
folderid=
